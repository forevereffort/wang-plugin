// import "normalize.css/normalize.css";
import "./main.scss";

const a = (x, y) => x + y;
const b = a(1, 2);

alert(b);
alert("this is a book");

// Bad
const html =
  "<p>The sum of " +
  a +
  " and " +
  b +
  " plus " +
  c +
  " is " +
  (a + b + c) +
  "</p>";

let aa = elements
  .addClass("foo")
  .children()
  .html("hello")
  .end()
  .appendTo("body");

foo(
  reallyLongArg(),
  omgSoManyParameters(),
  IShouldRefactorThis(),
  isThereSeriouslyAnotherOne()
);

a(x1, x2, x3, x4, x5);

const obj = {
  ready: 9,
  when: 4,
  "you are": 15,
  "you are": 15,
  "you are": 15,
  "you are": 15,
  "you are": 15,
  "you are": 15,
};

let foo = true;
let bar = false;
let a1;
let b2;
let c3;
